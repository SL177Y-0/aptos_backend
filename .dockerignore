# Ignore dependencies, as they are installed inside the container
node_modules

# Ignore build artifacts and temporary files
uploads

# Ignore environment files, as secrets should be passed at runtime
.env

# Ignore Git files
.git
.gitignore

# Ignore Docker files
Dockerfile
